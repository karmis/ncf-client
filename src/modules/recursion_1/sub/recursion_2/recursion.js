define(
[
	'../../recursion',
],
function(recursion)
{
	debugger;
	App.Modules.recursion_2 = App.module("recursion.recursion", function(){

		//App.appContentRegion.show(new views);

	});
	return App.Modules.recursion_2;
});