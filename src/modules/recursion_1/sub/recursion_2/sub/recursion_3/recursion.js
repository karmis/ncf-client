define(
[
	'../../recursion',
],
function()
{
	debugger;
	App.Modules.recursion_3 = App.module("recursion.recursion.recursion", function(){

		// App.appCaptionRegion.show(views);

	});
	return App.Modules.recursion_3;
});